$( function() {
    $( ".resizableRightMenu" ).resizable({
        resizeHeight: false,
        handles: "w",
    });
} );

$( function() {
    $( ".resizableLeftMenu" ).resizable({
        resizeHeight: false,
        handles: "e",
    });
} );


